# Announcement - что делаем дальше

Команда, после ревизии документации остались только реальные задачи:

1. **GraphSerializer (JSON)** - без этого бессмысленно пилить UI. Берёмся сразу после ревью этого документа.
2. **VS Code webview skeleton** - заводим `src/extension.ts`, отображаем пустой граф, учимся отправлять сообщения.
3. **CodeGen интерфейс** - хотя бы заглушка `ICodeGenerator` с одной реализацией `CppStubGenerator`, чтобы команда UI могла вызвать «Generate Code» и получить текст.

Пока не трогаем:
- AI, автокомплит, живой предпросмотр.
- Совместное редактирование и облачные функции.

Все детали - в `Документы/ProjectStatus.md` и `Dokументы/Архитектура/README.md`. Если добавляете новую идею - сначала код, потом текст.
