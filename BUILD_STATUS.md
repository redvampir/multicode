# Build / Infra Status

| Компонент | Статус | Детали |
| --- | --- | --- |
| CMake | ✅ | `cmake -S . -B build && cmake --build build` - собирает `multicode_core` и тесты. |
| Тесты | ✅ | `ctest --test-dir build` запускает Catch2. Покрытие - базовое. |
| GitHub Actions | ⚠️ | `.github/workflows/cpp-build.yml` и `code-format.yml` лежат в репо, но badges в README нет, запуск не проверен. |
| Форматирование | ⚠️ | Есть `.clang-format`, но нет скрипта/таска. Нужно подключить в CI. |
| VS Code extension build | 🚫 | `vscode-extension` не содержит исходников → `npm run compile` упадёт. |
| vcpkg | ✅ | 14.11.2025: GitHub Actions закрепляют `vcpkg` на 10b7a178346f3f0abef60cecd5130e295afd8da4 и ставят компиляторы до `run-vcpkg`. |
| Кодировка исходников | ✅ | 14.11.2025: include/visprog/core, src/core, tests, «Документы», `vscode-extension` — все текстовые файлы в UTF-8 без BOM; бинарные артефакты (`icon.png`, `.jekyll-metadata`, `mappings.wasm`) не трогаем. |

## Ближайшие действия
1. Проверить `cpp-build.yml` вручную (`act` или через временный тэг).
2. Добавить `clang-format`/`clang-tidy` шаги.
3. Когда появится UI, настроить `npm ci && npm run lint` в отдельном workflow.
