# Аудит Локализации — Результаты

> **Дата:** 2025-12-07
> **Фаза:** Sprint 1, Фаза 0
> **Статус:** Завершено

---

## 📋 Сводка

**Всего найдено hardcoded строк:** 15
- **Критичные (блокируют переключение RU/EN):** 9
- **Некритичные (console warnings):** 6

---

## 🔴 КРИТИЧНЫЕ — Требуют немедленного исправления

### 1. GraphEditor.tsx — Palette Items (строки 1207-1212)

**Проблема:** Названия узлов захардкожены в массиве `items`

**Текущий код:**
```typescript
const items: Array<{ key: string; label: string; type: GraphNodeType; presetLabel: string }> = [
  { key: 'function', label: 'Функция', type: 'Function', presetLabel: 'Функция' },
  { key: 'branch', label: 'Ветвление', type: 'Custom', presetLabel: 'Branch / Ветвление' },
  { key: 'switch', label: 'Switch', type: 'Custom', presetLabel: 'Switch / Переключатель' },
  { key: 'sequence', label: 'Последовательность', type: 'Custom', presetLabel: 'Sequence' },
  { key: 'variable', label: 'Переменная', type: 'Variable', presetLabel: 'Переменная' },
  { key: 'comment', label: 'Комментарий', type: 'Custom', presetLabel: 'Комментарий' }
];
```

**Недостающие TranslationKeys:**
- `palette.node.function`
- `palette.node.branch`
- `palette.node.switch`
- `palette.node.sequence`
- `palette.node.variable`
- `palette.node.comment`

**Приоритет:** 🔴 Критичный

---

### 2. GraphEditor.tsx — Palette Title (строка 1239)

**Проблема:** Заголовок палитры не использует translate

**Текущий код:**
```tsx
<div style={{ fontWeight: 700, color: theme.ui.panelTitle }}>
  Быстрое добавление (A / двойной клик)
</div>
```

**Недостающие TranslationKeys:**
- `palette.title` → 'Быстрое добавление' / 'Quick Add'
- `palette.hint` → '(A / двойной клик)' / '(A / double-click)'

**Приоритет:** 🔴 Критичный

---

### 3. GraphEditor.tsx — Palette Close Button (строка 1269)

**Проблема:** Кнопка "Закрыть" не переведена

**Текущий код:**
```tsx
<button type="button" onClick={closePalette} ...>
  Закрыть
</button>
```

**Недостающий TranslationKey:**
- `palette.close` → 'Закрыть' / 'Close'

**Приоритет:** 🔴 Критичный

---

### 4. GraphEditor.tsx — Minimap Alt Text (строка 1393)

**Проблема:** Alt текст миникарты захардкожен

**Текущий код:**
```tsx
<img src={miniMap.src} alt="Миникарта" ... />
```

**Недостающий TranslationKey:**
- `minimap.alt` → 'Миникарта' / 'Minimap'

**Приоритет:** 🟡 Средний (accessibility)

---

### 5. main.tsx — TranslationActions Title (строка 454)

**Проблема:** Заголовок панели перевода не использует translate

**Текущий код:**
```tsx
<div className="panel-title">Перевод графа</div>
```

**Недостающий TranslationKey:**
- `translation.title` → 'Перевод графа' / 'Graph Translation'

**Приоритет:** 🔴 Критичный

---

### 6. main.tsx — TranslationActions Label (строка 457)

**Проблема:** Label "Направление" не переведен

**Текущий код:**
```tsx
<div className="panel-label">Направление</div>
```

**Недостающий TranslationKey:**
- `translation.direction` → 'Направление' / 'Direction'

**Приоритет:** 🔴 Критичный

---

### 7. main.tsx — TranslationActions Button (строка 470)

**Проблема:** Текст кнопки перевода захардкожен

**Текущий код:**
```tsx
{pending ? 'Перевод...' : 'Перевести'}
```

**Недостающие TranslationKeys:**
- `translation.translate` → 'Перевести' / 'Translate'
- `translation.translating` → 'Перевод...' / 'Translating...'

**Приоритет:** 🔴 Критичный

---

### 8. store.ts — Copy Suffix (строка 368)

**Проблема:** Суффикс "(копия)" при вставке узлов захардкожен

**Текущий код:**
```typescript
label: node.label ? `${node.label} (копия)` : node.label,
```

**Недостающий TranslationKey:**
- `node.copySuffix` → '(копия)' / '(copy)'

**Решение:**
Нужно передать translate функцию в store или использовать getTranslation напрямую

**Приоритет:** 🟡 Средний

---

### 9. storeActions.ts — Default Node Label (строка 81)

**Проблема:** Дефолтное имя узла "Узел N" не переведено

**Текущий код:**
```typescript
label: payload.label?.trim() || `Узел ${graph.nodes.length + 1}`,
```

**Недостающий TranslationKey:**
- `node.defaultName` → 'Узел {number}' / 'Node {number}'

**Решение:**
Передать translate в storeActions или использовать getTranslation

**Приоритет:** 🟡 Средний

---

## 🟡 НЕКРИТИЧНЫЕ — Console Warnings

Эти строки не влияют на UI, но хорошо бы тоже перевести для единообразия:

| Файл | Строка | Текст |
|------|--------|-------|
| main.tsx | 54 | 'Не удалось загрузить настройки лэйаута из localStorage' |
| main.tsx | 110 | 'Невозможно отправить сообщение в расширение' |
| main.tsx | 595 | 'Игнорируем сообщение из неизвестного окна' |
| main.tsx | 604 | 'Игнорируем сообщение с неподдерживаемым origin' |
| main.tsx | 612 | 'Некорректное сообщение от расширения' |
| main.tsx | 701 | 'Не удалось сохранить настройки лэйаута' |

**Решение:** Можно оставить на русском (это debug messages) или добавить TranslationKeys для полноты.

**Приоритет:** 🟢 Низкий

---

## 📊 Недостающие TranslationKeys — Сводка

### Обязательные (для Фазы 1.1):

```typescript
// Palette
'palette.title': 'Быстрое добавление' / 'Quick Add'
'palette.hint': '(A / двойной клик)' / '(A / double-click)'
'palette.close': 'Закрыть' / 'Close'
'palette.node.function': 'Функция' / 'Function'
'palette.node.branch': 'Ветвление' / 'Branch'
'palette.node.switch': 'Переключатель' / 'Switch'
'palette.node.sequence': 'Последовательность' / 'Sequence'
'palette.node.variable': 'Переменная' / 'Variable'
'palette.node.comment': 'Комментарий' / 'Comment'

// Translation Panel
'translation.title': 'Перевод графа' / 'Graph Translation'
'translation.direction': 'Направление' / 'Direction'
'translation.translate': 'Перевести' / 'Translate'
'translation.translating': 'Перевод...' / 'Translating...'

// Minimap
'minimap.alt': 'Миникарта' / 'Minimap'

// Node operations
'node.copySuffix': '(копия)' / '(copy)'
'node.defaultName': 'Узел {number}' / 'Node {number}'
```

**Итого:** 16 новых ключей

---

## ✅ Критерии Приемки Фазы 0

- [x] Найдены все hardcoded строки в webview компонентах
- [x] Составлен список с указанием файла и строки
- [x] Приоритеты расставлены (🔴 Критичный / 🟡 Средний / 🟢 Низкий)
- [x] Создан чек-лист недостающих TranslationKeys

---

## 🎯 Следующий Шаг

**Фаза 1.1:** Расширение `translations.ts` — добавить 16 новых ключей

---

**Конец аудита.**
