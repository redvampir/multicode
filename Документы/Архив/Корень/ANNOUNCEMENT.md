# Announcement - что делаем дальше

Команда, после ревизии документации остались только реальные задачи:

1. **VS Code webview skeleton** - заводим `src/extension.ts`, отображаем пустой граф, учимся отправлять сообщения.
2. **CodeGen интерфейс** - хотя бы заглушка `ICodeGenerator` с одной реализацией `CppStubGenerator`, чтобы команда UI могла вызвать «Generate Code» и получить текст.
3. **CLI/CI интеграция сериализации** - отдельная команда сохранения/загрузки JSON + подключение `cpp-build.yml` в GitHub Actions.

Пока не трогаем:
- AI, автокомплит, живой предпросмотр.
- Совместное редактирование и облачные функции.

Все детали - в `Документы/ProjectStatus.md` и `Документы/Архитектура/README.md`. Если добавляете новую идею - сначала код, потом текст.
