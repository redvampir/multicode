# Build / Infra Status

| Компонент | Статус | Детали |
|-----------|--------|---------|
| CMake | ✅ | `cmake -S . -B build && cmake --build build` — собирает `multicode_core` и тесты. UTF-8 защита активна. |
| Тесты | ✅ | `ctest --test-dir build` запускает Catch2. Покрытие базовое. |
| Кодировка | ✅ | Защита от проблем с кириллицей: `scripts/build-cmake-utf8.ps1`. См. [ENCODING_ISSUE.md](Документы/Инфраструктура/ENCODING_ISSUE.md) |
| GitHub Actions | ⚠️ | `.github/workflows/cpp-build.yml` обновлён с UTF-8 защитой. Badges в README нет, запуск не проверен. |
| Форматирование | ⚠️ | Есть `.clang-format`, но нет скрипта/таска. Нужно подключить в CI. |
| VS Code extension build | 🚫 | `vscode-extension` не содержит исходников → `npm run compile` упадёт. |
| vcpkg | ⚠️ | `vcpkg.json` есть, но lockfile/manifest не синхронизирован с CI. |
| Кодировка исходников | ✅ | Все текстовые файлы в UTF-8 без BOM; бинарные артефакты не изменяем. |

## Ближайшие действия
1. Проверить `cpp-build.yml` вручную (`act` или через временный тэг).
2. Добавить `clang-format`/`clang-tidy` шаги.
3. Когда появится UI, настроить `npm ci && npm run lint` в отдельном workflow.
