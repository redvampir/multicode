# Advanced Features (без розовых пони)

Ниже - список идей, которые встречались в обсуждениях. У каждой есть статус: `✅ готово`, `🟡 в работе`, `⚪ не начинали`. Реализованы только то, что помечено галочкой.

| Фича | Статус | Реальность |
| --- | --- | --- |
| AI-нейминг узлов | ⚪ | Ничего нет. Нужно API (OpenAI/Ollama) и хранилище подсказок. |
| Live preview кода | ⚪ | Нужен генератор кода + интеграция в webview. |
| Mixed-language графы | ⚪ | Пока поддерживается только enum `Language`, но нет API переключения. |
| Автоматический поиск оптимизаций | ⚪ | Нет даже эвристик. |
| Undo/Redo | ⚪ | Нет UI, соответственно и истории действий. |
| Совместное редактирование | ⚪ | Нужно сетевое взаимодействие, не планировалось. |
| Экспорт в диаграммы | ⚪ | Можно сделать на базе Cytoscape, но пока нечего экспортировать. |
| Theme API | ⚪ | Параметр `multicode.theme` уже есть в `package.json`, но он ни на что не влияет. |
| Отладка (breakpoints на узлах) | ⚪ | Нет рантайма → нечего дебажить. |
| Инспектор производительности | ⚪ | Нужно хотя бы исполнение графов. |

**Итого:** пока нет ни одной «advanced» функции. Сначала реализуем базовый редактор + генератор кода, потом вернёмся к этому списку.
