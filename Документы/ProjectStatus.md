# Project Status (ноябрь 2025)

## Оглавление
- [Сводка](#сводка)
- [Детали](#детали)
- [Риски на ближайшие 2 недели](#риски-на-ближайшие-2-недели)
- [Следующие шаги](#следующие-шаги)

## Сводка
- ✅ Core library стабильна, тесты зелёные.
- ✅ JSON-сериализация (`GraphSerializer`) закрыта снапшотами и защищена `schema`-версированием.
- ⚠️ VS Code расширение не реализовано: нет UI, нет генерации.
- 🚫 Advanced features (AI, preview и т.д.) не в работе.

## Детали

| Направление | Статус | Комментарий |
|-------------|--------|-------------|
| C++ ядро | ✅ | `multicode_core` собирается, тесты покрывают Nodes/Ports/Graph. |
| Сериализация | ✅ | `GraphSerializer` импортирует/экспортирует JSON, проверяет заголовок `schema`, снапшот-тест `tests/core/test_graph_serializer.cpp`. |
| Генерация кода | ⚠️ | Нет ни интерфейса, ни заглушки. |
| VS Code UI | 🚫 | Пустая папка `vscode-extension/src`. |
| Документация | ✅ | Очищена от маркетинга, содержит факты. |
| CI | ⚠️ | GitHub Actions файлы есть, но не настроены (нужны секреты/баджи). |

## Риски на ближайший период
1. **Нет UI для редактора** — невозможно протестировать граф визуально, работа ограничена тестами.
2. **Нет общего формата узлов** — NodeFactory жёстко прошита enum-ами, пакеты подключить нельзя.
3. **CI не активирован** — GitHub Actions файлы есть, но не проверены в реальном окружении.

## Следующие шаги
1. Поднять минимальный webview, чтобы можно было добавлять узлы и связи.
2. Добавить CLI-команду/утилиту сохранения графа и включить `cpp-build.yml`.
3. Сформировать стратегию миграций JSON (schema v1.0.0 уже активна) и подготовить CLI-команду «graph migrate».
